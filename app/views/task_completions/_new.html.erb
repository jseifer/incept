<%= simple_form_for @task_completion, :html => { :class => 'form-inline' } do |f| %>
  <%= f.input_field :complete %>
  <%= f.button :submit, "Update" %>
  <%= f.input :task_id, :as => "hidden", :input_html => {:value => task.id}%>
  <%= f.input_field :active_minutes%>

  
<% end %>
